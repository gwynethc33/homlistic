build-job:
  stage: build
  tags:
    - homlistic
  script:
    - echo "Hello v2, $GITLAB_USER_LOGIN!" 
    - /home/gitlab-runner/dev/flutter/bin/flutter build web

deploy-prod:
  stage: deploy
  tags:
    - homlistic
  script:
    - echo "This job deploys something from the $CI_COMMIT_BRANCH branch."
  environment: production